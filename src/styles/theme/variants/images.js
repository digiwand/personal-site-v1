const images = {
  tech: {
    height: 48,
    transition: 'filter 0.3s, transform 0.3s',

    '&:hover': {
      cursor: 'pointer',
      transform: 'scale(1.18)',
    },
  },
};

export default images;
